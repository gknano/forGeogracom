import React from 'react';
import styled from '@emotion/styled';

const FormPageStyle = styled.div`
    transform: translate(50%,50%);
    position: absolute
    top:40%;
    left: 30%;
`;

const FormPage = () => {
    return(
        <FormPageStyle>FormPage</FormPageStyle>
    );
};

export default FormPage;